import React from "react";

const style: React.CSSProperties = {
  backgroundColor: "green"
};

function Header() {
  return (
    <div style={style}>
      Header
    </div>
  );
}

export default Header;